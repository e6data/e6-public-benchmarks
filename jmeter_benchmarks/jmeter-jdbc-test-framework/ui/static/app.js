const state={config:null,runs:[],poll:null};
const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function api(path,options={}){let r;try{r=await fetch(path,{headers:{'Content-Type':'application/json'},...options})}catch(e){throw new Error('UI backend unavailable. Check that ./run_ui.sh is still running; see logs/ui.log.')}const data=await r.json();if(!r.ok)throw new Error(data.error||r.statusText);return data}
function options(items,label=x=>x,value=x=>x){return items.map(x=>`<option value="${esc(value(x))}">${esc(label(x))}</option>`).join('')}
function engineCard(i){return `<div class="card engine"><div class="card-title"><span>0${i+1}</span><div><h3>${i?'Comparison engine':'Primary engine'}</h3><p>Credentials remain in the selected local file</p></div></div><div class="fields"><label>Display label<input class="engine-label" value="${i?'Engine B':'Engine A'}"></label><label>Connection profile<select class="connection" required><option value="">Select a connection…</option>${options(state.config.connections,x=>x.split('/').pop())}</select></label></div></div>`}
function renderEngines(){const count=$('parallel').checked?2:1;$('engines').className='engine-grid'+(count===2?' parallel':'');$('engines').innerHTML=Array.from({length:count},(_,i)=>engineCard(i)).join('')}
function planChanged(){const p=$('plan').value;document.querySelector('.profile-field').classList.toggle('hidden',!p.includes('arrivals')&&!p.includes('variable_concurrency'));document.querySelector('.param.concurrency').classList.toggle('hidden',!p.includes('concurrency')&&p!=='jdbc_run_once'&&!p.includes('variable'));document.querySelector('.param.qps').classList.toggle('hidden',p!=='jdbc_qps');document.querySelector('.param.qpm').classList.toggle('hidden',p!=='jdbc_qpm');$('durationUnit').textContent=p==='jdbc_qpm'?'minutes':'seconds';const recycle=$('RECYCLE_ON_EOF');const runOnce=p.endsWith('run_once');recycle.checked=!runOnce;recycle.disabled=runOnce;recycle.closest('label').title=runOnce?'Run-once always stops at the end of the query file':'';const profile=$('load_profile');if(p.includes('variable_concurrency')){[...profile.options].forEach(o=>o.hidden=!o.value.includes('utg_'))}else{[...profile.options].forEach(o=>o.hidden=o.value.includes('utg_'))}if(profile.selectedOptions[0]?.hidden)profile.selectedIndex=[...profile.options].findIndex(o=>!o.hidden)}
async function init(){state.config=await api('/api/config');$('plan').innerHTML=options(state.config.plans,x=>x.label,x=>x.id);$('query_file').innerHTML='<option value="">Select a query dataset…</option>'+options(state.config.queries,x=>x.split('/').pop());$('load_profile').innerHTML=options(state.config.profiles,x=>x.split('/').pop());renderEngines();planChanged();await refreshReports();await refreshRuns()}
document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab,.panel').forEach(x=>x.classList.remove('active'));b.classList.add('active');$(b.dataset.tab).classList.add('active');if(b.dataset.tab==='compare')refreshReports()});
$('parallel').onchange=renderEngines;$('plan').onchange=planChanged;$('refreshRuns').onclick=refreshRuns;
$('runForm').onsubmit=async e=>{e.preventDefault();$('formError').textContent='';const plan=$('plan').value;const common={plan,query_file:$('query_file').value,load_profile:$('load_profile').value,CONCURRENT_QUERY_COUNT:+$('CONCURRENT_QUERY_COUNT').value,QPS:+$('QPS').value,QPM:+$('QPM').value,HOLD_PERIOD:+$('HOLD_PERIOD').value,MAX_CONCURRANCY:+$('MAX_CONCURRANCY').value,MAX_ERROR_PCT:+$('MAX_ERROR_PCT').value,RECYCLE_ON_EOF:plan.endsWith('run_once')?false:$('RECYCLE_ON_EOF').checked,RANDOM_ORDER:$('RANDOM_ORDER').checked};const cards=[...document.querySelectorAll('.engine')];const runs=cards.map(card=>({...common,label:card.querySelector('.engine-label').value,connection:card.querySelector('.connection').value}));try{await api('/api/runs',{method:'POST',body:JSON.stringify({runs})});document.querySelector('[data-tab="live"]').click();await refreshRuns()}catch(err){$('formError').textContent=err.message}}
function fmt(v,suffix=''){return v===null||v===undefined?'—':`${v}${suffix}`}
function chart(series){const sets=[['in_flight',series?.in_flight||[],'var(--purple-600)'],['arrivals',series?.arrivals||[],'var(--green)'],['latency_ms',series?.latency_ms||[],'var(--amber)']];const length=Math.max(...sets.map(x=>x[1].length),0);if(!length)return'<div class="chart-empty">Waiting for completed samples…</div>';const lines=sets.map(([name,values,color])=>{const max=Math.max(...values,1);const pts=values.map((v,i)=>`${i/(values.length-1||1)*300},${70-v/max*64}`).join(' ');return `<polyline class="${name}" style="stroke:${color}" points="${pts}"/>`}).join('');return `<div class="chart-wrap"><div class="chart-title"><strong>Live workload shape</strong><span>normalized trends · JMeter CSV</span></div><svg class="spark" viewBox="0 0 300 75" preserveAspectRatio="none">${lines}</svg><div class="legend"><span class="violet">In flight</span><span class="green">Arrivals/sec</span><span class="amber">Avg latency</span></div></div>`}
function planLabel(id){return state.config?.plans.find(p=>p.id===id)?.label||id}
function runContract(r,m){const c=r.config||{},meta=r.summary?.meta||{};let target='—',expected='—';if(c.plan==='jdbc_qps'){target=`${c.QPS} QPS`;expected=`≈ ${c.QPS*c.HOLD_PERIOD} arrivals`}else if(c.plan==='jdbc_qpm'){target=`${c.QPM} QPM`;expected=`≈ ${c.QPM*c.HOLD_PERIOD} arrivals`}else if(c.plan.includes('arrivals')){target='CSV arrival schedule';expected=r.summary?.load_profile?.expected?`${r.summary.load_profile.expected} arrivals`:c.load_profile?.split('/').pop()}else if(c.plan.includes('variable_concurrency')){target='CSV concurrency schedule';expected=c.load_profile?.split('/').pop()}else if(c.plan.includes('concurrency')){target=`${c.CONCURRENT_QUERY_COUNT} concurrent`;expected=`hold ${c.HOLD_PERIOD}s`}else{target=`${c.CONCURRENT_QUERY_COUNT||1} worker`;expected='query file once'}const actualDuration=r.summary?.wall_clock_s;const adherence=r.summary?.load_profile;let matched='—';if(adherence?.kind==='arrivals')matched=`${adherence.delivered}/${adherence.expected} (${adherence.delivered_pct}%)`;if(adherence?.kind==='concurrency')matched=`${adherence.matched_pct}% profile match`;return {target,expected,duration:`${c.HOLD_PERIOD}${c.plan==='jdbc_qpm'?' min':' sec'}`,actual:actualDuration!==undefined?`${actualDuration}s`:'running',matched}}
function runCard(r){const m=r.summary||r.metrics||{};const latency=r.summary?.latency_ms||{};const samples=m.samples??0,failed=m.failed??0,throughput=m.throughput_per_s??m.throughput??0,p95=latency.p95??m.p95;const liveSeries=r.metrics?.series||{};const series=r.summary?{in_flight:r.summary.in_flight_per_s||[],arrivals:r.summary.arrivals_per_s||[],latency_ms:liveSeries.latency_ms||[]}:liveSeries;const contract=runContract(r,m);const failure=r.summary?.failure_messages?.[0]||r.metrics?.top_failure;const threshold=r.config.MAX_ERROR_PCT??5;const reason=r.status==='failed'?(failure?`${failure.count}× ${failure.message}`:`Run exceeded the ${threshold}% error threshold or the runner exited non-zero.`):'';return `<article class="run-card"><div class="run-head"><div><span class="run-type">${esc(planLabel(r.config.plan))}</span><h3>${esc(r.label)}</h3><small>${esc(r.config.connection?.split('/').pop())} · ${esc(r.config.query_file?.split('/').pop())}</small></div><span class="status ${r.status}">${esc(r.status)}</span></div><div class="contract"><div><span>Requested load</span><strong>${esc(contract.target)}</strong></div><div><span>Planned volume</span><strong>${esc(contract.expected)}</strong></div><div><span>Planned duration</span><strong>${esc(contract.duration)}</strong></div><div><span>Actual duration</span><strong>${esc(contract.actual)}</strong></div><div><span>Profile delivery</span><strong>${esc(contract.matched)}</strong></div><div><span>Error threshold</span><strong>${esc(threshold)}%</strong></div></div><div class="metrics"><div class="metric"><strong>${samples}</strong><span>query samples</span></div><div class="metric"><strong>${fmt(throughput,'/s')}</strong><span>achieved throughput</span></div><div class="metric"><strong>${fmt(p95,' ms')}</strong><span>p95 latency</span></div><div class="metric"><strong>${failed}</strong><span>errors</span></div><div class="metric"><strong>${m.peak_in_flight??m.active??0}</strong><span>peak in flight</span></div><div class="metric"><strong>${m.error_pct!==undefined?fmt(m.error_pct,'%'):'—'}</strong><span>error rate</span></div></div>${reason?`<div class="failure-reason"><strong>Why this run failed</strong><p>${esc(reason)}</p></div>`:''}${chart(series)}<div id="details-${r.id}"></div><details class="runner-output"><summary>Runner output</summary><div class="logs">${esc((r.logs||[]).slice(-30).join('\n'))}</div></details><div class="run-actions">${r.report_id?`<button class="secondary" onclick="loadDetails('${r.id}','${esc(r.report_id)}')">Per-query results</button>`:''}${r.status==='running'?`<button class="danger" onclick="cancelRun('${r.id}')">Cancel run</button>`:''}</div></article>`}
async function refreshRuns(){try{state.runs=await api('/api/runs');$('liveCount').textContent=state.runs.filter(r=>r.status==='running').length;const el=$('liveRuns');el.className=state.runs.length?'run-grid':'run-grid empty';el.innerHTML=state.runs.length?state.runs.map(runCard).join(''):'No runs launched in this UI session.';if(state.runs.some(r=>['running','queued'].includes(r.status))&&!state.poll)state.poll=setInterval(refreshRuns,2000);if(!state.runs.some(r=>['running','queued'].includes(r.status))&&state.poll){clearInterval(state.poll);state.poll=null;refreshReports()}}catch(e){$('liveRuns').textContent=e.message}}
async function cancelRun(id){if(confirm('Stop this JMeter run?')){await api(`/api/runs/${id}/cancel`,{method:'POST',body:'{}'});refreshRuns()}}
window.cancelRun=cancelRun;
async function loadDetails(runId,reportId){const el=$(`details-${runId}`);el.innerHTML='<div class="chart-empty">Loading detailed results…</div>';try{const d=await api(`/api/report-details?id=${encodeURIComponent(reportId)}`);const rows=d.per_query.map(q=>`<tr><td>${esc(q.label)}</td><td>${q.samples}</td><td>${q.failed}</td><td>${fmt(q.mean_ms)}</td><td>${fmt(q.p50_ms)}</td><td>${fmt(q.p95_ms)}</td><td>${fmt(q.p99_ms)}</td><td>${fmt(q.max_ms)}</td></tr>`).join('');el.innerHTML=`<div class="detail-results"><div class="chart-title"><strong>Per-query latency</strong><span>${d.per_query.length} labels · milliseconds</span></div><div class="table-scroll"><table class="compare-table"><thead><tr><th>Query</th><th>Samples</th><th>Errors</th><th>Mean</th><th>p50</th><th>p95</th><th>p99</th><th>Max</th></tr></thead><tbody>${rows}</tbody></table></div><p class="artifact-list">Artifacts: ${d.artifacts.map(esc).join(' · ')}</p></div>`}catch(e){el.innerHTML=`<div class="failure-reason"><p>${esc(e.message)}</p></div>`}}
window.loadDetails=loadDetails;
function reportLabel(r){const m=r.summary.meta||{};return `${m.engine||m.run_type||'run'} · ${m.test_plan||''} · ${r.id}`}
async function refreshReports(){const reports=await api('/api/reports');const html=options(reports,reportLabel,x=>x.id);$('leftReport').innerHTML=html;$('rightReport').innerHTML=html;if(reports.length>1)$('rightReport').selectedIndex=1}
$('compareButton').onclick=async()=>{try{const c=await api('/api/compare',{method:'POST',body:JSON.stringify({left:$('leftReport').value,right:$('rightReport').value})});renderComparison(c)}catch(e){$('comparison').textContent=e.message}}
function renderComparison(c){const names={throughput_per_s:'Throughput / sec',error_pct:'Error rate %',p50_ms:'p50 latency ms',p95_ms:'p95 latency ms',p99_ms:'p99 latency ms',peak_in_flight:'Peak in flight',drain_s:'Drain seconds'};const rows=Object.entries(c.metrics).map(([k,m])=>{const good=m.change_pct===null?'':((m.higher_is_better?m.change_pct>0:m.change_pct<0)?'better':'worse');return `<tr><td>${names[k]}</td><td>${fmt(m.left)}</td><td>${fmt(m.right)}</td><td class="${good}">${m.change_pct===null?'—':fmt(m.change_pct,'%')}</td></tr>`}).join('');const latency=['p50_ms','p95_ms','p99_ms'];const max=Math.max(...latency.flatMap(k=>[c.metrics[k].left,c.metrics[k].right]),1);const bars=latency.map(k=>`<div class="bar-card"><h3>${names[k]}</h3>${['left','right'].map(side=>`<div class="bar-row"><div class="bar-label"><span>${side}</span><b>${c.metrics[k][side]} ms</b></div><div class="bar-track"><div class="bar-fill" style="width:${c.metrics[k][side]/max*100}%"></div></div></div>`).join('')}</div>`).join('');const el=$('comparison');el.className='comparison';el.innerHTML=`<table class="compare-table"><thead><tr><th>Metric</th><th>Baseline</th><th>Candidate</th><th>Change</th></tr></thead><tbody>${rows}</tbody></table><div class="bars">${bars}</div>`}
init().catch(e=>{$('formError').textContent=`UI initialization failed: ${e.message}`});
